# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: prj.salescoordinator
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://192.168.14.195:8000
          client: "500"
        app:
          name: ZSALCOR
          description: Sales Coordinator
          package: ZSAP_FIORI
          transport:  DFSK900008
        exclude:
          - /test/
