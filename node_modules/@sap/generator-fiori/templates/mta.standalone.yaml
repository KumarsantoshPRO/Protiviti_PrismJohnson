_schema-version: "3.2"
ID: <%- id %>
description: <%- mtaDescription %>
version: <%- mtaVersion %>

build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install

parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo

modules: []

resources: []
